diff --git a/Makefile b/Makefile
index df7debe..ddfbde3 100644
--- a/Makefile
+++ b/Makefile
@@ -246,7 +246,7 @@ ifeq ($(HOSTARCH),$(ARCH))
 CROSS_COMPILE ?=
 endif
 
-export CROSS_COMPILE=aarch64-none-elf-
+export CROSS_COMPILE=aarch64-elf-
 
 KCONFIG_CONFIG	?= .config
 export KCONFIG_CONFIG
